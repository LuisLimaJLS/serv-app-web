import { PayToTextPipe } from './pay-to-text.pipe';

describe('PayToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PayToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
