import { StateToTextPipe } from './state-to-text.pipe';

describe('StateToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StateToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
